import { TextInput, Checkbox } from 'evergreen-ui';

const FormTesting = () => {
  return (
    <div>
      <TextInput name='textInput' placeholder='text input' />
      <Checkbox name='checkbox' label='checkbox' />
    </div>
  )
};

export default FormTesting;
